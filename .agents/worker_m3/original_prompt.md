## 2026-05-23T13:31:22Z
请执行 PatentX UI/UX 全景重塑里程碑 M3：欢迎向导与注意力焦点锁定。

具体任务：
1. **重构欢迎向导 (`UploadHub.tsx`)**：
   - 首次进入 (IDLE 状态) 时，在主卡片核心区域以流光打字机渐显核心定位文案：“基于多 Agent 嵌套辩论的欧洲专利局 EPO 诊断与特征对齐评估系统”。
   - 在打字机效果显示完毕后，平滑 Morphing 渐显三个平行的三步指引指示卡片：
     *   `① 拖入文档或输入 Claim`
     *   `② 观察 Agent 推理辩论与黑板特征矩阵`
     *   `③ 必要时介入批注冲突并一键 Resume`
   - 为方便用户“输入 Claim”，需要在 `UploadHub` 内增加一个精美的文本输入框（Textarea）用于输入 Claim，并保留拖拽文件的触发逻辑。
   - 当用户开始在文本框中输入，或发生拖拽文件（drag over/enter）动作时，下方的三个指引卡片应通过 Framer Motion 平滑坍塌收缩并自动隐去，以减少认知负荷。
2. **重构多 Agent 辩论流 (`DiagnosticDashboard.tsx`)**：
   - 废弃原本的黑框 monospaced 终端输出样式，改用精美的对话气泡（Chat Bubbles）流式向上推移渲染。
   - 标明发言 Agent 的中文身份（如 “法官 Agent”、“新颖性审查员”、“申请人代理”等），并精致展示对应的物理大模型标签（如 `GPT-4` / `Claude-3` 等）。
   - 你需要解析 `debate_logs` 中的文本字符串（例如 `[epo_examiner (Claude-3)]: ...` 或类似的日志格式），提取出 Agent 角色、模型标签和具体的发言正文。
3. **特征对齐决策矩阵优化 (`DiagnosticDashboard.tsx`)**：
   - 对齐状态标签采用彩色微光小标签（微光玻璃拟态，配合微渐变发光）精致呈现。
   - 对矩阵行（国内申请特征）分配并显式展示唯一特征坐标 ID（如 `DF_0`, `DF_1` 等）。
4. **实现活动流与矩阵焦点同步锁定**：
   - 当分析正在进行中（SSE 正在流动，状态为 Thinking/Debating）时，提取最新辩论日志提及的国内特征 ID（如匹配正则 `/DF_(\d+)/`）。
   - 让对齐决策矩阵中对应的国内特征行（例如 `DF_0` 行）和左侧对话气泡同时触发同步 high profile 锁定发光效果（四周亮起浅蓝色微光阴影，其余行变暗），精准引导用户注意力，避免产生认知迷茫。
5. **打包构建验证**：
   - 在 `frontend` 目录下运行 `npm run build` 命令，验证所有修改的代码在打包时没有任何 TypeScript 类型报错或语法报错。
6. 将你修改的文件、具体设计、前端构建命令与结果，详细写入你的工作目录（`.agents/worker_m3/`）下的 `handoff.md` 中。
7. **语言规则**：你必须始终使用【简体中文】编写代码注释、提交说明和 handoff。
